platform :ios, '9.0'

use_frameworks!

# ignore all warnings from all pods
inhibit_all_warnings!

target :LBPWWDC do
    pod 'MagicalRecord', '~> 2.3'
    pod 'SDWebImage', '~> 3.7'
end


target :LBPWWDCTests do
    pod 'Specta', '~> 1.0'
    pod 'Expecta', '~> 1.0'
    pod 'OCMock', '~> 3.2'
end
